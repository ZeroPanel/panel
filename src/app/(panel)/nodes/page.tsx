import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function NodesPage() {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>Nodes</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground">Nodes page is under construction.</p>
        </CardContent>
      </Card>
    </div>
  );
}
